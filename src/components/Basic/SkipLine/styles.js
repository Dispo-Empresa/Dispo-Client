import styled from 'styled-components'

// ALTERAR O NOME > SkipLine n faz mt sentido

export const SkipLine = styled.div`
display: flex;
align-items: center;
padding-top: ${props => props.paddingTop}px;
`